<?xml version="1.0" encoding="UTF-8"?>
<p>We recommend running DDRP on a server or computer with multicore functionality because certain processes are very memory intensive and may execute slowly or stall without parallel processing. The platform was designed to run on a Linux server, but we have also successfully used it on a Windows OS with eight cores. As an example, a model that was run on a Linux server with 48 cores completed 
 <italic>ca</italic>. 3.5 times faster than it did on a Windows OS with eight cores.
</p>

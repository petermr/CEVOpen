0: In our case, only three different orthomosaics are available; hence, we decided to use transfer learning by loading a pre-trained ResNet50 architecture with weights from ImageNet, due to its accuracy and reduced training time.
1: ImageNet is one of the largest image databases for image classification research, with more than 80,000 labels and at least 1000 images for each label.

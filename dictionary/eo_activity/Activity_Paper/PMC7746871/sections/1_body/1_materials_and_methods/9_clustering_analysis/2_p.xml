<?xml version="1.0" encoding="UTF-8"?>
<p>The 
 <italic>k</italic>-means algorithm is a clustering method based on partitioning which is capable of effectively dealing with large-scale data but is easy to understand (
 <xref rid="B13" ref-type="bibr">Diao, 2012</xref>). Contents of the two measured constructs and yields of the fresh roots were combined into a data frame. The R-tree indexing algorithm was then used to analyze the spatial index in the 
 <italic>k</italic>-means clustering method.
</p>

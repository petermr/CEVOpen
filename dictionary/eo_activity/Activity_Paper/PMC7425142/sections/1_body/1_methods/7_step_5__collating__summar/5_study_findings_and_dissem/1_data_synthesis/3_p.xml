<?xml version="1.0" encoding="UTF-8"?>
<p id="Par69">A separate table will include SDRs and will include the same fields as above, plus the metric for detection (the definition of signal will be replaced by the specified threshold for significance and the database will replace the field “setting”).</p>
